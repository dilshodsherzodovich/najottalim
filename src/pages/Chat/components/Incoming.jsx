import React from "react";

const Incoming = ({ children }) => {
  return <p className="text-[#667085] text-[25px] font-[700] bg-white w-[200px] text-center rounded-[20px]">{children}</p>;
};

export default Incoming;
